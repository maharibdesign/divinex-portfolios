---
const { session } = Astro.locals;
---
<header>
  <nav>
    <a href="/" class="brand-text">Divinex</a>
    <div class="nav-actions">
      {session ? (
        <>
          <a href="/profile/edit" class="nav-button">Edit Profile</a>
          <form action="/api/auth/logout" method="POST">
            <button type="submit" class="nav-button secondary">Log Out</button>
          </form>
        </>
      ) : (
        <>
          <!-- Both buttons now point to the single login page -->
          <a href="/login" class="nav-button secondary">Log In</a>
          <a href="/login" class="nav-button">Sign Up</a>
        </>
      )}
      <button id="theme-toggle" title="Toggle theme">
        <!-- SVG Icons here, unchanged -->
      </button>
    </div>
  </nav>
</header>
<script>
  // The theme toggle script is unchanged
</script>
<style>
  /* All navbar styles are unchanged */
</style>